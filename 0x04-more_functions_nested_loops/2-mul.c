#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 * @a: takes in an integer
 * @b: takes in an integer
 * Return: result of a*b
 */
int mul(int a, int b)
{
	int a;
	int b;

	return (a * b);
}
